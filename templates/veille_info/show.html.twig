{% extends 'base.html.twig' %}

{% block title %}VeilleInfo{% endblock %}

{% block body %}
<style>
    .btn_rouge{
        color: #fff;
        background-color: red;
        padding: 6px 6px;
        border-radius: 10px;
        margin-left:auto;
        margin-right:auto;
}
</style>

    <h1>{{ veille_info.titre }}</h1>
    
    <p>{{ veille_info.description}}<p>
        <br>
    <p>Lien pour découvrir l'article : <a href="{{ veille_info.lien }}">ici</a><p>
        <br>
    <p>Notation : {{ veille_info.note }}<p>
            


    <a href="{{ path('veille_info_edit', {'id': veille_info.id}) }}"><i class="fas fa-edit"></i></a>

    {{ include('veille_info/_delete_form.html.twig') }}
        <br>
    <a href="{{ path('veille_info_index') }}">Revenir à la liste</a>
{% endblock %}
