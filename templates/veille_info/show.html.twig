{% extends 'base.html.twig' %}

{% block title %}Veille Info
{% endblock %}

{% block body %}
	<style>
		.form-action {
			display: inline;
		}
		.bouton-suppr {
			margin-top: -38px;
			margin-left: 167px;

		}

		.page-contenu {
			margin: 15px;

		}
	</style>

<div class="page-contenu">
		<h1 class="h3 mb-3 font-weight-normal text-center">Aperçu de ({{ veille_info.titre }})</h1>


		<table class="table">
			<tbody>
			<tr>
					<th>Titre</th>
					<td>{{ veille_info.titre }}</td>
				</tr>
				<tr>
					<th>Lien</th>
					<td><a href="{{ veille_info.lien }}">{{ veille_info.lien }}</a></td>
				</tr>
				<tr>
					<th>Description</th>
					<td>{{ veille_info.description }}</td>
				</tr>
				<tr>
					<th>Note</th>
					<td>{{ veille_info.note }}</td>
				</tr>
			</tbody>
		</table>

	<div class="form-action">
			<a href="{{ path('veille_info_index') }}">
				<button class="btn btn-success">Retour</button>
			</a>

			<a href="{{ path('veille_info_edit', {'id': veille_info.id}) }}">
				<button class="btn btn-info">Modifier</button>
			</a>

			{{ include('veille_info/_delete_form.html.twig') }}
		</div>
	</div>
	{% endblock %}